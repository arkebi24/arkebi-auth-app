const Joi = require('joi');


const signupValidation = (req, res, next) => {
    
}

const loginValidation = (req, res, next) => {
    
}
module.exports = {
    signupValidation,
    loginValidation
}